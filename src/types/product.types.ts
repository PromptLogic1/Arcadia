export interface Product {
  id: string
  name: string
  slug: string
  description: string
  price: number
  // Add other product fields as needed
}

export interface ProductRow {
  id: string
  name: string
  slug: string
  description: string
  price: number
} 